To run the native test against JDK's HttpClient:
`./gradlew run --args='native'`

To run the test against OkHttpClient:
`./gradlew run --args='okhttp'`

To run the test against apache http client:
`./gradlew run --args='apache'`